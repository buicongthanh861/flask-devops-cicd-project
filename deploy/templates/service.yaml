apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.microservice.name }}-service
  namespace: {{ .Values.microservice.namespace }}
  labels:
    app: {{ .Values.microservice.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.microservice.name }}
  ports:
    - name: http
      port: 80          # port Service
      targetPort: {{ .Values.workload.port }}  # port trong container
